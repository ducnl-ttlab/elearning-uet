import { user as userEn } from './en/user.en';
import { user as userVi } from './vi/user.vi';

export const userLocale = {
    en: {
        ...userEn,
    },
    vi: {
        ...userVi,
    },
};
