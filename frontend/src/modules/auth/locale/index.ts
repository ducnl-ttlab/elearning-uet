import { auth as authEn } from './en/auth.en';
import { auth as authVi } from './vi/auth.vi';

export const authLocale = {
    en: {
        ...authEn,
    },
    vi: {
        ...authVi,
    },
};
